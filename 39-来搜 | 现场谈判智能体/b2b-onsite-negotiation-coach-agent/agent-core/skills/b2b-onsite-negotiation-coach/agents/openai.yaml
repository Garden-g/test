interface:
  display_name: "现场谈判教练"
  short_description: "围绕目标、筹码与授权边界准备谈判问题、交换条件和会后动作"
  default_prompt: "使用 $b2b-onsite-negotiation-coach 为这次客户面谈制定谈判路线、问题清单、让步边界和会后跟进表格。"

dependencies:
  tools:
    - type: "mcp"
      value: "accio"
      description: "Accio ask_user, file reading, web search and workbook delivery tools"

policy:
  allow_implicit_invocation: true
