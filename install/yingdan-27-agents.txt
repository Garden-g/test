Install the Yingdan suite of 27 agents into the current Accio Work space.

Use Chinese for all user-facing communication, including progress updates, clarification questions, error messages, and the final installation report.

ZIP:
https://raw.githubusercontent.com/Garden-g/test/main/release/yingdan-27-accio-agents-v1.2.0.zip

SHA-256:
https://raw.githubusercontent.com/Garden-g/test/main/release/yingdan-27-accio-agents-v1.2.0.zip.sha256

Download the ZIP, verify its SHA-256 checksum, and extract it with UTF-8 filename support. Read INSTALL_PROMPT.txt inside the package, then run the installer for the current operating system: installer/install-windows.ps1 on Windows or installer/install-macos.sh on macOS. Do not use the Create Agent workflow, and do not overwrite existing agents.

The names of all 27 agents must begin with “赢单｜”, and every agent must use the same black-and-orange Yingdan logo. The installer must merge the local user profile and memory from the current primary Accio agent into all 27 agents. If an older agent from the same source already exists, preserve its ID and directory, update its name and logo in place, and do not create a duplicate. Report success only when the installer outputs INSTALL_OK, installed plus skipped equals 27, branded equals 27, personalized equals 27, and no .installing-* directory remains. Do not restart Accio automatically. When installation is complete, ask me to fully quit and reopen Accio Work manually.
