# 来搜 Accio Work 课程智能体套装

本套装包含课程实际使用的 47 个外贸业务智能体，覆盖市场与选品、国际站运营、营销素材、客户开发、销售转化和经营管理等场景，支持 Windows 和 macOS。

## 安装

1. 打开 [安装提示词](INSTALL_PROMPT.md)，复制对应系统的简短命令并发送给 Accio Work。
2. 等待 Accio Work 自动下载并完成安装。
3. 安装成功后，完整退出并重新打开 Accio Work，即可在智能体列表中使用。

安装器会把 47 个智能体一次性装入当前 Accio Work 个人或团队空间。所有名称统一为 `来搜 | {智能体名称}`，并使用同一份来搜 Logo；已有同来源旧版智能体会保留原 ID 原位升级，不会重复创建。

## v2.5.0 Logo 回退

- 47 个智能体头像全部改回原版 `481×480` 透明底来搜 Logo，不再使用 v2.3.0 的白底方形版本。
- 原版 Logo SHA-256 为 `4487f0325a41e1d410c241b59b9c3a5de2c90e2f632171154198bd4710442813`。
- 从 v2.4.0 升级时保留原 Agent ID、用户画像、长期记忆、课程映射和私有 Skill，只原位更新头像与模板版本。

## v2.4.0 市场客户分析 Excel 交付修正

- `3-1 来搜 | 市场和客户定位分析智能体` 已加入鸡公老师提供的 TWS 完整研判报告 Excel 验收样本，样本 SHA-256 为 `51013f6a8e1c6784c3e4ccace09ce7d84b40dcb0aa3db0b533fcb0aa5c04e3bf`。
- 正式研判报告现在强制生成 8 个工作表：报告总览、行业市场数据底盘、客户群体超详细拆解、客户群体概览、目标市场区域优先级、合规标准红线、机会赛道与落地布局、推荐产品表。
- 新增来源回溯、企业输入核对、产品图片、FOB/MOQ、公式错误、工作簿重开、包结构和视觉校验要求；缺资料时必须补问或标注草稿，不得编造。
- 采用逐 Agent 模板版本判断，从 v2.3.0 升级时只替换发生变化的 3-1 模板，其余 46 个智能体内容不变，同时保留原 Agent ID、用户画像和记忆。

## v2.3.0 头像修正

- 47 个智能体头像全部替换为 `1000×1000` 纯白底方形来搜 Logo，并保留圆形头像裁切安全边距，避免横版 Logo 在 Accio Work 中被挤压或裁切。
- Logo 本体使用原始像素等比例缩放，没有重绘文字、图形或品牌色。
- 从 v2.2.0 升级时保留原 Agent ID、私有 Skill、账号画像和长期记忆，只原位更新头像与版本信息。
- 新 Logo SHA-256 为 `3a06620b21997b3f2dcb87b2e4b12599b72fc9dffc1a3eb607ce73c9cda6d544`。

## v2.2.0 修正

- 已按正式课程表锁定鸡公老师负责的 14 项：`3-1` 至 `3-6`、`4-1` 至 `4-6`、`4-9`、`4-10`，这些 Agent 全部改用鸡公老师原始资料，不混用来搜 GitHub 已发布或未发布 Skill。
- 安装器现已支持一个 Agent 绑定一个或多个私有 Skill；广告询盘联合诊断、图像生成和店铺装修等组合能力不会再漏装第二个 Skill。
- 从 v2.1.0 升级时保留原 Agent ID、账号画像和长期记忆，同时原位替换旧模板及私有 Skill，不创建同名重复 Agent。
- 47 个智能体继续统一使用新来搜 Logo，Logo SHA-256 为 `caaeab365f9ae75384bc7131f639d247e864f74133ee13200d4a6a8ad88fcc05`。

## v2.1.0 修正

- `来搜 | 优爆品提升规划智能体` 已替换为鸡公老师 V5 `alibaba-explosive-booster` 私有 Skill，不再使用通用商品经营复盘 Skill。
- 47 个智能体的头像统一替换为新来搜 Logo，Logo SHA-256 为 `caaeab365f9ae75384bc7131f639d247e864f74133ee13200d4a6a8ad88fcc05`。
- 已安装的旧版优爆品智能体会利用原 `sourceAgentId` 原位替换内核，避免出现同名重复智能体。
