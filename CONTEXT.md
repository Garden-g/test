# 来搜课程智能体安装包说明

## 模块用途

本目录保存“外贸 AI 智能体系统落地”课程实际使用的来搜品牌智能体。它是磐西合作交付项目内的冻结副本，用于安装、录课、验收和后续追溯，不是标准母版的维护入口。

## 使用入口

- `AGENT_REGISTRY.json`：查看全部 47 个智能体、来源状态、课程编号、插件依赖、私有 Skill 和 ZIP 校验值。
- `COURSE_AGENT_MAP.json`：从课程小节反查需要安装或演示的智能体；同一小节可以对应多个智能体。
- `01-来搜 | .../` 至 `47-来搜 | .../`：每个智能体的独立交付目录。
- 每个编号目录内的 `INSTALL_PROMPT.md`：把安装指令交给能够读取本地 ZIP 的 Accio Work Agent。
- `品牌资产/来搜-logo.png`：本批智能体统一使用的 Logo 源文件副本。

## 目录结构

每个编号目录包含四类内容：

1. `*-agent.zip`：可安装的 Accio Work 智能体包。
2. `*-agent/`：与 ZIP 内容一致的展开目录，便于人工审阅；目录内的 `profile.jsonc.id` 才是实际的 `MID-*` 智能体 ID。
3. `INSTALL_PROMPT.md`：安装和验收指令。
4. `BUILD_MANIFEST.json`：智能体 ID、名称、课程编号、来源状态、依赖和校验值。

ZIP 根目录固定包含 `profile.jsonc`、`agent-core/` 和 `permissions/`。智能体专属 Skill 位于 `agent-core/skills/`，不得拆出后安装到全局 Skill 目录。

## 命名、品牌和映射规则

- 智能体显示名固定使用 `来搜 | {智能体名称}`。
- 头像固定使用来搜 Logo；清单中的 Logo SHA-256 用于确认 47 个包没有混入其他品牌头像。
- A48:F50 是录制层面的合并安排，不代表单个智能体。实际映射仍按各业务场景拆分，并记录在 `COURSE_AGENT_MAP.json`。
- 同一个智能体可被多个课程小节复用，因此课程引用数量可能大于智能体数量。

## 来源与维护边界

- `GitHub已发布` 是用户已经批准的最新标准母版。
- `GitHub未发布` 是仍待批准的候选标准母版。
- 本目录中的内容是从上述两处复制或构建出的项目交付副本，不得反向覆盖、移动或重命名源目录内容。
- 通用能力需要调整时，应回到 `GitHub未发布` 更新候选母版；只属于来搜交付的品牌内容留在本项目中。

## 更新与验证

新增或替换智能体时，应同步更新 `AGENT_REGISTRY.json` 和 `COURSE_AGENT_MAP.json`，并逐包执行官方生成器的 `--validate-only` 校验。还需确认：

- 目录数、ZIP 数和总清单数量一致。
- 所有智能体 ID 唯一，显示名符合来搜命名格式。
- 所有头像均可解码且与来搜 Logo 校验值一致。
- ZIP 内没有凭据、私钥、原作者绝对路径或临时文件。
- 安装前目标空间不存在同名 ID，安装后完整退出并重启 Accio Work，再验收名称、Logo、描述、插件依赖和独立对话页。
